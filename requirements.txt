# NovaMind — Complete Requirements
# Install with: pip install -r requirements.txt

# ── Core HTTP / LLM ──────────────────────────────────────
requests==2.34.2
httpx==0.27.0

# ── IPC & Serialization ──────────────────────────────────
cbor2==5.6.3

# ── Testing ──────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0

# ── GUI ──────────────────────────────────────────────────
PyQt6

# ── Desktop Automation (REQUIRED for MS Paint drawing) ───
pyautogui
pygetwindow==0.0.9        # Window focus / position detection
pyperclip==1.9.0          # Clipboard read/write
comtypes==1.4.1           # Windows UI Automation (UIA)  [win]
pywin32==306              # Windows Job Objects & OS containment
pydantic==2.13.4

# ── Vision / OCR ─────────────────────────────────────────
Pillow
pytesseract       # Requires Tesseract installed separately
easyocr            # Alternative OCR (no Tesseract needed)
opencv-python      # UI element detection + template matching

# ── Numeric / Data ────────────────────────────────────────
numpy
pandas

# ── Embeddings / Semantic Search ─────────────────────────
sentence-transformers

# ── System Monitoring ────────────────────────────────────
psutil==5.9.8

# ── Browser Automation ───────────────────────────────────
playwright        # After install: playwright install chromium
selenium          # Alternative to Playwright

# ── 3D Game (Nova Mindscape) ─────────────────────────────
ursina

# ── LLM Providers ────────────────────────────────────────
groq
together
openai             # Used for OpenRouter + XAI + Gemini compat endpoints
google-generativeai

# ── Code Formatting ──────────────────────────────────────
black
autopep8

# ── Utilities ────────────────────────────────────────────
python-dotenv

# ── Optional extras ──────────────────────────────────────
# pypdf2           # PDF reading in file_agent
# docx2txt         # Word doc reading
# openpyxl         # Excel reading
# markdown         # Markdown rendering in UI
flask
