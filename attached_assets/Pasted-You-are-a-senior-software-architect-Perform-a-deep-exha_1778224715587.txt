You are a senior software architect. Perform a deep, exhaustive audit of this entire codebase for my Desktop Agent project. Cover every aspect below and be specific — reference actual file names, function names, line numbers, and code snippets where relevant.

---

## 1. PROJECT OVERVIEW
- What does this project do at a high level?
- What is the core architecture? (e.g., event-driven, modular, monolithic)
- What are the main entry points?
- What tech stack is being used? (languages, frameworks, libraries, versions)

---

## 2. FOLDER & FILE STRUCTURE AUDIT
- Map out the complete folder structure
- Identify what each folder/file is responsible for
- Flag any files that seem misplaced, redundant, or poorly named
- Are there any orphaned files or dead code files?

---

## 3. CORE FUNCTIONALITY AUDIT
- What are the core features currently implemented?
- What is working correctly?
- What is partially implemented or broken?
- What features are missing but seem intended based on the code?

---

## 4. CODE QUALITY AUDIT
- Identify anti-patterns, bad practices, or code smells
- Are there any hardcoded values that should be config/env variables?
- Is there duplicate or redundant logic that should be abstracted?
- Are functions/classes doing too much? (violation of single responsibility)
- Is the code readable and well-commented?

---

## 5. ARCHITECTURE & DESIGN AUDIT
- Is the architecture scalable for a desktop agent?
- Are concerns properly separated? (UI, logic, data, agent core)
- How is state managed across the app?
- How are modules communicating with each other? Any tight coupling?
- Is there a clear agent loop / task execution pipeline?

---

## 6. AI / AGENT LOGIC AUDIT
- How is the AI/LLM integrated? (API calls, local model, etc.)
- How does the agent plan, reason, and execute tasks?
- Is there a memory system? How does it work?
- How does the agent handle tool use / function calling?
- Are prompts hardcoded or dynamic? Are they well-structured?
- Is there error recovery if the agent fails a task?

---

## 7. DESKTOP INTEGRATION AUDIT
- How does the agent interact with the desktop? (mouse, keyboard, screen, apps)
- Which OS APIs or automation libraries are being used?
- Are there any platform-specific limitations or hardcoded OS assumptions?
- How is screen capture or UI parsing handled?

---

## 8. PERFORMANCE AUDIT
- Are there any obvious bottlenecks or slow operations?
- Is there unnecessary blocking code that should be async?
- Are API calls batched or optimized?
- Any memory leaks or large objects being held unnecessarily?

---

## 9. SECURITY AUDIT
- Are API keys or secrets properly handled? (env vars, not hardcoded)
- Is user data being stored safely?
- Are there any dangerous system-level operations with no safeguards?
- Is there any input sanitization where needed?

---

## 10. ERROR HANDLING & LOGGING AUDIT
- Is there consistent error handling throughout?
- Are errors caught and handled gracefully or do they crash the app?
- Is there a logging system? Is it useful and well-structured?
- Are there any silent failures (errors caught but not reported)?

---

## 11. DEPENDENCIES AUDIT
- List all external dependencies
- Are any dependencies outdated, unmaintained, or unnecessary?
- Are there any dependency conflicts?
- Is the dependency list minimal and justified?

---

## 12. TESTING AUDIT
- Is there any test coverage? (unit, integration, e2e)
- What critical paths have zero test coverage?
- What is the hardest part of this codebase to test and why?

---

## 13. WHAT'S MISSING / INCOMPLETE
- List everything that appears to be planned but not yet built
- List all TODOs, FIXMEs, or placeholder comments in the code
- What would need to be built to make this production-ready?

---

## 14. PRIORITY ACTION PLAN
Based on everything above, give me:
- 🔴 Critical issues (must fix immediately)
- 🟡 Important issues (fix soon)
- 🟢 Nice to have improvements
- 📋 A suggested order of what to work on next

---

Be brutally honest. Don't skip anything. I want to know the real state of this codebase.